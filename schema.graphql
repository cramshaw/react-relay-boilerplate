type Mutation {
  login(username: String!, password: String!): User
  easyLogin(username: String!): User
}

type Query {
  hello: String
  username: String
  user: User
}

type User {
  username: String
  password: String
  token: String
}
